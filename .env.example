# Copy this file to .env and adjust if your MySQL setup differs from
# XAMPP's defaults (host=localhost, user=root, no password).

DB_HOST=localhost
DB_PORT=3306
DB_NAME=bluebookers
DB_USER=root
DB_PASS=
